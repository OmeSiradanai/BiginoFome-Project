using System.Collections;
using System.Collections.Generic;
using UnityEngine;


/*public class MaleeBastState : State
{
    //how log this state should be active for before moving on
    public float duration;
    //Cached animator component
    protected Animator animator;
    //Bool to check whether or not the next actack in the sequence should be played or not\
    protected bool shouldCombo;
    //The attack index in the sequence of attack 
    protected int attackIndex;

    public override void OnEnter(StateMachine _stateMachine)
    {
        base.OnEnter(_stateMachine);
    }
}
*/